module.exports = {
  // if process.env port isn't defined, use 4000
  port: process.env.PORT || 4000
};

// heroku can now change where this application runs when you deploy it


// a single place where you make a decision of how to read it. could use .env instead 